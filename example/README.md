# SAF Picker Demo UI

Modern UI pages to exercise the saf-picker plugin on Android.

## Run in the browser (UI only)

```bash
npm install
npm run dev
```

## Run on Android

```bash
npm install
npm run sync
npx cap add android
npm run open:android
```

The SAF picker buttons only work on Android. In the browser, the UI renders with a warning.
